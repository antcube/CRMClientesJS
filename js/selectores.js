// Seleccionamos el elemento de entrada para el nombre y lo exportamos
export const nombreInput = document.querySelector('#nombre');

// Seleccionamos el elemento de entrada para el email y lo exportamos
export const emailInput = document.querySelector('#email');

// Seleccionamos el elemento de entrada para el teléfono y lo exportamos
export const telefonoInput = document.querySelector('#telefono');

// Seleccionamos el elemento de entrada para la empresa y lo exportamos
export const empresaInput = document.querySelector('#empresa');

// Seleccionamos el formulario y lo exportamos
export const formulario = document.querySelector('#formulario');

// Seleccionamos el listado de clientes y lo exportamos
export const listadoClientes = document.querySelector('#listado-clientes');